= form_for @example1 do |f|
  - if @example1.errors.any?
    #error_explanation
      %h2= "#{pluralize(@example1.errors.count, "error")} prohibited this example1 from being saved:"
      %ul
        - @example1.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :age
    = f.number_field :age
  .actions
    = f.submit 'Save'
